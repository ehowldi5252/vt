**Card** — the white, hairline-bordered, 12px-rounded surface under every block in the app.

```jsx
<Card padding="md">…</Card>
<Card padding="sm" elevated>…</Card>
```

`padding` = `sm` / `md` / `lg`; `elevated` adds the soft purple-tinted shadow (used sparingly).
